# Optional: enables OpenAI extraction during `npm run dev` only.
# The key is read by the Vite dev server — never exposed in the built static app.
OPENAI_API_KEY=your_key here
# Optional model (default: gpt-4o-mini)
# OPENAI_EXTRACT_MODEL=gpt-4o-mini
